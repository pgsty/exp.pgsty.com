# Landing page counters. These are product facts, not adoption numbers. Every
# value must remain traceable to a source page in this site.

# Approximate metric-family count exposed by the default merged config.
metric_families: 600

# YAML definition files shipped under pg_exporter/config/.
collector_files: 58

# Default collector branches cover PostgreSQL 10 through 19+.
postgres_range: "10-19+"

# Git tags represented by one release-note post per language.
tagged_versions: 35
